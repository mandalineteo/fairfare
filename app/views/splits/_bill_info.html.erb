<div class="container">
  <div class="mt-5">
  <%# @split.members.each do |member| %>
    <%# <%= #member.given_nickname(@split.user) %>
  <%# <% #end %>
  <h3>Bills for:</h3>
  <% @split.bills.each do |bill| %>
    <div class="my-5">
      <h5><%= bill.merchant %></h5>
    </div>
  <% end %>

  <h3>Settlements:</h3>
  <% @split.get_split_stats.each do |stat| %>
    <div class="my-5">
      <h5><%= "#{stat[:payee_member].given_nickname(current_user)} owes #{stat[:payer_member].given_nickname(current_user)} SGD#{stat[:amount]}" %></h5>
    </div>
  <% end %>
</div>
