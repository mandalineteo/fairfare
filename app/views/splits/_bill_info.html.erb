<div class="container">
  <div class="mt-5">
  <%# @split.members.each do |member| %>
    <%# <%= #member.given_nickname(@split.user) %>
  <%# <% #end %>
  <h3>Bills for:</h3>
  <% @split.bills.each do |bill| %>
    <div class="my-5">
      <h4><%= bill.merchant %></h4>
      <h5><%= "Date: #{bill.date}" %></h5>
      <h5><%= "Total amount: SGD#{format('%.2f', bill.total_amount/100.00)}" %></h5>

    </div>
  <% end %>

  <h3>Settlements:</h3>
  <% @split.settlements.each do |settlement| %>
    <div class="my-4">
      <%= "#{settlement[:payee].phone_number} owes #{settlement[:payer].phone_number} SGD#{settlement[:amount]/100.00}" %>
    </div>
  <% end %>
  <%# @split.get_split_stats.each do |stat| %>
    <div class="my-5">
      <h5><%#= "#{stat[:payee_member].given_nickname(current_user)} owes #{stat[:payer_member].given_nickname(current_user)} SGD#{stat[:amount]/100.00}" %></h5>
    </div>
  <%# end %>
</div>
